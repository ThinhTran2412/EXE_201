# Tiến độ ngày 14/05/2026

## Session sáng
- Tái thiết kế trang quản lý dịch vụ và giá theo phong cách nghệ thuật hơn.
- Làm rõ các card gói dịch vụ, chỉnh màu chữ và nền để dễ đọc hơn.
- Sửa hiển thị `Quote` trong hồ sơ nhiếp ảnh gia để không bị cắt nội dung.
- Sửa backend hồ sơ nhiếp ảnh gia để giữ `PasswordHash` và lưu `Quote` đúng xuống database.
- Bật Swagger để dễ truy cập và kiểm tra API.
- Cập nhật `README.md` sang tiếng Việt và trỏ tới tài liệu trong `Vault_ShootMatch`.

## Session chiều — Migration & Personal Info Feature

### Backend
- **Fix migration chưa apply**: Migration `20260514183000_AddPhotographerPersonalInfo` đã được tạo nhưng chưa apply lên Supabase → chạy `dotnet ef database update` để apply thành công.
- Các cột mới thêm vào bảng `photographers`: `NationalId`, `PersonalAddress`, `VerificationDocumentFrontUrl`, `VerificationDocumentBackUrl`, `VerificationPortraitUrl`.

### Mobile — Bugs đã fix
- **GraphQL query thiếu fields**: Query `photographerProfile` không fetch `nationalId`, `personalAddress`, `verificationDocumentFrontUrl/Back/Portrait` → data luôn `null` dù đã lưu DB. **Fix**: thêm đủ fields vào query.
- **Field name mismatch**: Interface dùng `address` nhưng domain model và GraphQL expose là `personalAddress` → đổi toàn bộ sang `personalAddress` cho nhất quán.
- **TypeScript implicit any**: 4 callback `onChangeText` trong `PersonalInfoScreen` thiếu `: string` type annotation → thêm vào để xóa lỗi TS.
- **`locked = true` mặc định**: Các field trong `PersonalInfoScreen` bị disabled khi vào trang → đổi mặc định thành `locked = false`.
- **`(profile as any)` casts**: Bỏ các cast thừa sau khi interface đã được cập nhật đầy đủ.

## Ghi chú
- Nếu ngày sau tiếp tục làm việc, tạo thêm một file `.md` mới theo đúng định dạng ngày-tháng-năm tiếng Việt.
- Nội dung file nên ngắn gọn, ghi lại những thay đổi chính trong ngày.
